import { FactoryConfig } from '../../base'

import { WidgetOptions } from './widget-options'

export const createWidget = (_config: FactoryConfig, _options: WidgetOptions) => {
  //
  return {
    refresh: () => {},
  }
}
